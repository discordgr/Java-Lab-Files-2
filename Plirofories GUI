import java.awt.*;       // Using AWT containers and components
import java.awt.event.*; // Using AWT events and listener interfaces
import javax.swing.*;    // Using Swing components and containers

public class test extends JFrame 
{
		Container cp = getContentPane();
		cp.setLayout(new FlowLayout());   //Flowlayout gia grammiki diataxi panw ston upodoxea, BorderLayout gia na xwrisoume ton upodoxea
		
		/* 
		cp.add(new JLabel());
		label.setIcon(C:\Users\p3150109\Desktop\Java\Images); 
		
		paneButton -  JPanel but1,but2,but3 (add,remove,details)  leitourgei san upo-upodoxeas
		paneButton.add(but1); etc
		
		resultArea - JtextArea
		
		Sto simeio pou emfanizetai i lista  = list - JList , listModel - DefaultListModel 
		listModel.addElement(item.getTitle())
		
		Auti i lista tha mpei mesa se ena antikeimeno listScroller tupou JScrollPane (gia na mporei na anebokatebainei (an i lista einai megali))
		
		cp.add(paneButton LINE_START);
		cp.add(listScroller CENTER);
		cp.add(label LINE_END);
		
		paneButton.addActionListener (new ActionListener() 
		{	
         	public void actionPerformed(ActionEvent e) 
			{
				++count;
				tfCount.setText(""+count);
			}
		}
    );
		*/
		
		
